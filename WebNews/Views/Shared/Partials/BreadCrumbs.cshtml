@using EPiServer.Core
@model WebNews.Models.ViewModels.IPageViewModel<WebNews.Models.Pages.BaseEditorialPage>

    @foreach (var page in Model.PageCrumbs)
    {

    if (ContentReference.StartPage.ID != page.PageLink.ID)
    {
        <b>--></b>
    }
    @Html.ContentLink(page)
    }
